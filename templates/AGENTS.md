# Agent Instructions

## Product contract

Read `AI_NATIVE_PLATFORM.yaml` before changing code. Keep all declared capabilities, evidence paths, tests, examples, and documentation synchronized.

## Required checks

Run the repository's formatter, linter, strict type checker, test suite, documentation checks, examples, and:

```bash
ai-native validate AI_NATIVE_PLATFORM.yaml
```

## Governance

Agents may prepare scoped branches and pull requests. Human approval is mandatory for breaking, security, credential, public API, permission, and release changes.
