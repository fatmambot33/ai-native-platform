name: Codex review governance

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review, edited, labeled]
  pull_request_target:
    types: [labeled]
  pull_request_review:
    types: [submitted, dismissed]
  pull_request_review_thread:
    types: [resolved, unresolved]

permissions:
  contents: read

concurrency:
  group: codex-review-${{ github.event_name }}-${{ github.event.pull_request.number }}-${{ github.event.pull_request.head.sha }}-${{ github.event.pull_request.base.sha }}
  cancel-in-progress: false

jobs:
  request:
    if: github.event_name == 'pull_request_target' && github.event.action == 'labeled' && github.event.label.name == 'codex:review' && github.event.pull_request.draft == false
    permissions:
      actions: read
      contents: read
      issues: write
      pull-requests: read
    runs-on: ubuntu-latest
    steps:
      - name: Request one merge-ready Codex review
        uses: fatmambot33/ai-native-platform/actions/codex-review-gate@1b26da0ebed82de7589da135c5e641507d4dbc55
        with:
          token: ${{ github.token }}
          pr-number: ${{ github.event.pull_request.number }}
          head-sha: ${{ github.event.pull_request.head.sha }}
          base-sha: ${{ github.event.pull_request.base.sha }}
          mode: request
          request-label: codex:review

  codex-review:
    if: (github.event_name == 'pull_request' || github.event_name == 'pull_request_review' || github.event_name == 'pull_request_review_thread') && github.event.pull_request.draft == false
    permissions:
      actions: read
      contents: read
      issues: read
      pull-requests: read
    runs-on: ubuntu-latest
    steps:
      - name: Require Codex review of current HEAD and resolved threads
        uses: fatmambot33/ai-native-platform/actions/codex-review-gate@1b26da0ebed82de7589da135c5e641507d4dbc55
        with:
          token: ${{ github.token }}
          pr-number: ${{ github.event.pull_request.number }}
          head-sha: ${{ github.event.pull_request.head.sha }}
          base-sha: ${{ github.event.pull_request.base.sha }}
          mode: wait
          request-label: codex:review
